# Database connection settings
spring.datasource.url=jdbc:mysql://b47240d7043965:b64147d5@us-cdbr-iron-east-03.cleardb.net/heroku_2fbd2d7c225da2b?reconnect=true

spring.datasource.username=b47240d7043965
spring.datasource.password=b64147d5

# Specify the DBMS
spring.jpa.database = MYSQL

# Show or not log for each sql query
spring.jpa.show-sql = false

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# For Heroku deployment
server.port=${PORT:8080}